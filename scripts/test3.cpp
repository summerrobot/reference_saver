#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <stdlib.h>

using namespace std;

/* char *user = getenv("USER");
//string user_to_string= 
std::string str(user);
string OUTPUTFILENAME{"PostionLogger"};

struct Angles {
    double roll;
    double pitch;
    double yaw;
};


int main()
{
     // Open file

    std::ofstream file{ "/home/" + str +  "/Desktop/" + OUTPUTFILENAME + ".yaml" };


    if(!file)
    {
        cerr<<"Fault: File could not open"<<endl;
    }

    
    // Write position to file

    std::ostringstream sstream;
    sstream<< 2.0 << " " << 3.0 << " " << 2.0 <<" "<< 1.2<<endl;

    string getValues{sstream.str()};

    file << getValues;



    return 0;
    
} */
   